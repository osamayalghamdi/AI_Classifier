"""Cluster service — clustering engine (pipeline stages 30–40).

Pipeline position: services/cluster — offering exact-match + embedding
clustering, batch sub-offering proposals, pairwise verification.
"""
